structure:
  modules:
    a.py: |
      INVALID
args:
  --logdir: logs
  --outdir: out
  --log_level: DEBUG
logs:
  - "Unable to import module 'a': name 'INVALID' is not defined"
exception: SystemExit(1)