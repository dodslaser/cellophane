structure:
  schema.yaml: |
    properties:
      a:
        type: number
args:
  --a: "INVALID"
  --workdir: out
output:
  - "Invalid value for '--a': 'INVALID' is not a valid float."
exception: SystemExit(2)